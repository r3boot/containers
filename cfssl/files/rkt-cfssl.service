[Unit]
Description=Cfssl api process running inside rkt
Requires=network-online.target
After=networking.service

[Service]
EnvironmentFile=/etc/rkt-environment
Slice=machine.slice
ExecStartPre=/usr/local/bin/rkt-cleanup cfssl
ExecStart=/usr/bin/rkt \
--net=default:IP=${IP_CFSSL} \
--hostname=${FQDN_CFSSL} \
--dns=${UNBOUND_ANYCAST_IPV4} \
--dns-search="${DOMAIN}" \
--set-env-file=/etc/rkt-environment \
--volume=data,kind=host,source=/var/rkt/cfssl,readOnly=true \
run cfssl
ExecStopPost=/usr/local/bin/rkt-cleanup cfssl
KillMode=mixed
Restart=always

[Install]
WantedBy=multi-user.target
